# Models

Thư mục lưu model cục bộ khi cần chạy offline.

Các file thường dùng:

```text
yolo11n-pose.pt
yolo11n.pt
rtmpose-s.onnx
```

Mặc định Ultralytics và RTMLib tự tải model nếu cấu hình dùng tên model hoặc URL. Khi dùng model cục bộ, cập nhật đường dẫn trong `config.yaml`.
